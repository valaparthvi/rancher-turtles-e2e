# This workflow is a reusable one called by other workflows
name: (template) Rancher Turtles E2E tests

on:
  workflow_call:
    # Variables to set when calling this reusable workflow

    secrets:
      gcp_credentials:
        description: Credentials to use to connect
        required: true
      gcp_project:
        description: GCP Project name details
        required: true
      pat_token:
        # A token is needed to be able to add runner on the repo, maybe this can be changed later
        # This token is linked to a personal account
        # So in case of token issue you have to check (no specific order and for example):
        # - the expiration date
        # - if the account associated still exists
        # - if the person still has access to the repo
        description: PAT token used to add runner
        required: true
      aws_access_key:
        description: AWS_ACCESS_KEY_ID required to create AWS Cloud credentials.
        required: true
      aws_secret_key:
        description: AWS_SECRET_ACCESS_KEY required to create AWS Cloud credentials.
        required: true
      qase_api_token:
        description: Qase API token to use for Qase reporting
        required: true
      rancher_password:
        description: Rancher login password
        required: true
      azure_client_id:
        description: AZURE_CLIENT_ID is required to create Azure Cloud credentials
        required: true
      azure_client_secret:
        description: AZURE_CLIENT_SECRET is required to create Azure Cloud credentials
        required: true
      azure_subscription_id:
        description: AZURE_SUBSCRIPTION_ID is required to create Azure Cloud credentials
        required: true
      azure_tenant_id:
        description: AZURE_TENANT_ID is required to create Azure Cloud credentials
        required: true
      vsphere_secrets_json_base64:
        description: VSPHERE_SECRETS_JSON_BASE64 is required to create vSphere cluster
        required: true
      vsphere_endpoints_list:
        description: VSPHERE_ENDPOINTS_LIST contains space separated list of IPs for kube-vip
        required: true
      docker_auth_username:
          description: DOCKER_AUTH_USERNAME to perform docker login
          required: true
      docker_auth_password:
        description: DOCKER_AUTH_PASSWORD to perform docker login
        required: true
      docker_registry_config:
        description: DOCKER_REGISTRY_CONFIG to perform docker login
        required: true
      prime_registry:
        description: Prime registry to fetch images for prime releases
        required: true
      stg_prime_registry:
        description: Staging Prime registry to fetch images for prime pre-releases
        required: true
      prime_artifacts_url:
        description: URL to fetch artifacts list for prime releases
        required: true
      prime_head_chart_url:
        description: URL to fetch prime pre-release helm chart
        required: true
      log_encryption_key:
        description: Passphrase used to GPG-encrypt the uploaded test artifacts
        required: true
    inputs:
      cert-manager_version:
        description: Version of cert-manager to use
        type: string
      destroy_runner:
        description: Destroy the auto-generated self-hosted runner
        default: true
        type: boolean
      rancher_version:
        description: Rancher Manager version to use for installation
        type: string
      rancher_upgrade_version:
        description: Rancher Manager version to use for upgrade
        type: string
      turtles_dev_chart:
        description: Install rancher turtles dev chart
        required: true
        type: boolean
      runner_template:
        description: Runner template to use
        default: rancher-turtles-e2e-ci-spot-20260622165944418700000001
        type: string
      test_description:
        description: Short description of the test
        type: string
      upstream_cluster_version:
        description: Cluster upstream version where to install Rancher (K3s)
        default: v1.34.4+k3s1
        type: string
      zone:
        description: GCP zone to host the runner
        default: asia-south1-c
        type: string
      qase_report:
        description: Report to QASE
        default: false
        type: boolean
      grep_test_by_tag:
        description: Grep tags. For multiple selection separate with spaces
        required: true
        type: string
        default: '@install @short @full'
      skip_cluster_delete:
        description: Skip CAPI cluster and fleet repo deletion tests
        type: boolean
      cluster_name_suffix:
        description: Suffix to be used in cluster name (default - github.run_number)
        type: string
      target_build_type:
        description: Build type for turtles_dev_chart (prime|community)
        type: string
      skip_fleet_addon_installation:
        description: Skip Fleet Addon provider installation
        type: boolean
        default: false

jobs:
  determine-milestone:
    runs-on: ubuntu-latest
    continue-on-error: true
    outputs:
      milestone: ${{ steps.milestone.outputs.milestone }}
      milestone_id: ${{ steps.milestone.outputs.milestone_id }}
    steps:
      - name: Determine milestone from latest matching rancher/rancher release
        id: milestone
        env:
          GH_TOKEN: ${{ github.token }}
          QASE_API_TOKEN: ${{ secrets.qase_api_token }}
          QASE_PROJECT_CODE: RT
          QASE_REPORT: ${{ inputs.qase_report }}
          RANCHER_VERSION: ${{ inputs.rancher_version }}
        run: |
          CHANNEL="${RANCHER_VERSION%%/*}"
          VERSION="${RANCHER_VERSION#*/}"
          # Extract base version (e.g. "v2.15.1-<sha>-head" -> "2.15.1")
          # Examples: 2.15.10. v2.15, v2.15-1-head-bla
          if [[ "${VERSION}" =~ ^v?([0-9]+\.[0-9]+(\.[0-9]+)?) ]]; then
            # Rematch returns first () matching group
            VERSION="${BASH_REMATCH[1]}"
          fi
          # If no patch version found (for e.g. VERSION=2.15), calculate the latest patch version
          if [[ "${VERSION}" =~ ^[0-9]+\.[0-9]+$ ]]; then
            # Query the latest released patch for this minor version
            LATEST=$(gh api "repos/rancher/rancher/releases?per_page=100" \
              --jq '.[] | select(.prerelease == false) | .tag_name' | \
              { grep "^v${VERSION}\." || true; } | sort -V | tail -n 1)
            if [[ -z "${LATEST}" ]]; then
              LATEST_PATCH=-1
            else
              LATEST_PATCH="${LATEST##*.}"
            fi
            MILESTONE_TITLE="v${VERSION}.$(( LATEST_PATCH + 1 ))" # For not released yet: -1+1=0
          else
            MILESTONE_TITLE="v${VERSION}"
          fi

          # Append "-head" to the milestone title if the channel is "head"
          if [[ "${CHANNEL}" =~ head ]]; then
            MILESTONE_TITLE="${MILESTONE_TITLE}-head"
          fi

          echo "milestone=${MILESTONE_TITLE}" | tee -a "${GITHUB_OUTPUT}"

          # Finish here if QASE reporting not enabled
          if [ "${QASE_REPORT}" != "true" ]; then
            exit 0
          fi

          # 1. Try to find existing milestone ID
          MILESTONE_ID=$(
            curl -s --request GET \
              --url "https://api.qase.io/v1/milestone/${QASE_PROJECT_CODE}?search=${MILESTONE_TITLE}" \
              --header "Token: ${QASE_API_TOKEN}" \
              --header "accept: application/json" \
            | jq -r --arg TITLE "$MILESTONE_TITLE" '.result.entities[] | select(.title == $TITLE) | .id'
            )

          # 2. If not found, create it and get the new ID
          if [ -z "$MILESTONE_ID" ]; then
            echo "Creating new milestone ${MILESTONE_TITLE} in QASE"
            MILESTONE_ID=$(
              curl -s --request POST \
                --url "https://api.qase.io/v1/milestone/${QASE_PROJECT_CODE}" \
                --header "Token: ${QASE_API_TOKEN}" \
                --header "Content-Type: application/json" \
                --header "accept: application/json" \
                --data "$(jq -n --arg t "$MILESTONE_TITLE" '{title: $t, description: $t}')" \
              | jq -r '.result.id // empty'
              )
          fi

          echo "milestone_id=${MILESTONE_ID}" | tee -a "${GITHUB_OUTPUT}"

  determine-runner:
    runs-on: ubuntu-latest
    outputs:
      initial-runner: ${{ steps.set-runner.outputs.initial-runner }}
    steps:
      - name: Set Runner
        id: set-runner
        run: |
            if [[ "${{ inputs.grep_test_by_tag }}" =~ (@vsphere|@capv) ]]; then
              echo "initial-runner=vsphere" >> $GITHUB_OUTPUT
              if [[ "${{ inputs.grep_test_by_tag }}" =~ "@full" || "${{ inputs.grep_test_by_tag }}" =~ "@short" || "${{ inputs.grep_test_by_tag }}" =~ "@migration" ]]; then
                echo "ERROR: @vsphere tests cannot be combined with @full, @short and @migration"
                exit 1
              fi
            else
              echo "initial-runner=ubuntu-latest" >> $GITHUB_OUTPUT
            fi

  create-runner:
    needs: determine-runner
    runs-on: ${{ needs.determine-runner.outputs.initial-runner }}
    outputs:
      uuid: ${{ steps.generator.outputs.uuid }}
      runner: ${{ steps.generator.outputs.runner }}
      public_dns: ${{ steps.dns.outputs.public_dns }}
    steps:
      # actions/checkout MUST come before auth
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Generate UUID and Runner hostname
        id: generator
        run: |
          if [[ ${{ needs.determine-runner.outputs.initial-runner }} == "vsphere" ]]; then
            echo "uuid=${{ runner.name }}" >> ${GITHUB_OUTPUT}
            echo "runner=${{ runner.name }}" >> ${GITHUB_OUTPUT}
          else
            # NOTE: keep the runner name to less than 63 characters!
            UUID=$(uuidgen)
            GH_REPO_FULL=${{ github.repository }}
            GH_REPO=${GH_REPO_FULL#*/}
            echo "uuid=${UUID//-}" >> ${GITHUB_OUTPUT}
            echo "runner=${GH_REPO//\//-}-ci-${UUID//-}" >> ${GITHUB_OUTPUT}
          fi
      - name: Cleanup vsphere runner if needed
        if: ${{ needs.determine-runner.outputs.initial-runner == 'vsphere' }}
        run: |
          # Cleanup vsphere runner if needed but do not delete github.workspace
          if [ -f /usr/local/bin/k3s ]; then
            cd tests && make vsphere-runner-cleanup
          fi
      - name: Authenticate to GCP
        if: ${{ needs.determine-runner.outputs.initial-runner != 'vsphere' }}
        uses: google-github-actions/auth@7c6bc770dae815cd3e89ee6cdf493a5fab2cc093 # v3.0.0
        with:
          credentials_json: ${{ secrets.gcp_credentials }}
      - name: Setup gcloud
        if: ${{ needs.determine-runner.outputs.initial-runner != 'vsphere' }}
        uses: google-github-actions/setup-gcloud@aa5489c8933f4cc7a4f7d45035b3b1440c9c10db # v3.0.1
      - name: Create runner
        if: ${{ needs.determine-runner.outputs.initial-runner != 'vsphere' }}
        run: |
          gcloud compute instances create ${{ steps.generator.outputs.runner }} \
            --source-instance-template ${{ inputs.runner_template }} \
            --zone ${{ inputs.zone }} \
            --labels=owner=${{ github.actor }},team=highlander-qa,runner=${{ github.run_number }}
      - name: Create GCP secrets
        if: ${{ needs.determine-runner.outputs.initial-runner != 'vsphere' }}
        run: |
          echo -n ${{ secrets.pat_token }} \
            | gcloud secrets create PAT_TOKEN_${{ steps.generator.outputs.uuid }} --ttl="36000s" --quiet --data-file=-
          echo -n ${{ github.repository }} \
            | gcloud secrets create GH_REPO_${{ steps.generator.outputs.uuid }} --ttl="36000s" --quiet --data-file=-
      - name: Get public dns name in GCP
        id: dns
        run: |
          if [[ ${{ needs.determine-runner.outputs.initial-runner }} == "vsphere" ]]; then
            LOCAL_IP=$(ip route get 8.8.8.8 | awk '{print $7}')
            # Raise an error if IP is empty
            if [[ -z "${LOCAL_IP}" ]]; then
              echo "LOCAL_IP is empty!" >&2
              false
            fi
            echo "public_dns=${LOCAL_IP}.sslip.io" >> ${GITHUB_OUTPUT}
          else
            for ((i=0; i<10; i++)); do
              PUBLIC_IP=$(gcloud compute instances list --format="value(EXTERNAL_IP)" \
                --filter="name=${{ steps.generator.outputs.runner }}" 2> /dev/null)
              [[ -n "${PUBLIC_IP}" ]] && break
              sleep 2
            done
            PUBLIC_DNS=$(dig -x ${PUBLIC_IP} +short 2> /dev/null | sed 's/\.$//')
            echo "public_dns=${PUBLIC_DNS}" >> ${GITHUB_OUTPUT}
            # Raise an error if either IP and/or DNS are empty
            if [[ -z "${PUBLIC_IP}" || -z "${PUBLIC_DNS}" ]]; then
              echo "PUBLIC_IP and/or PUBLIC_DNS are empty!" >&2
              false
            fi
          fi

  e2e:
    needs: [create-runner,determine-milestone]
    runs-on: ${{ needs.create-runner.outputs.uuid }}
    outputs:
      qase_run_id: ${{ steps.qase_run_id.outputs.qase_run_id }}
    env:
      # Rancher versions with embedded turtles
      RANCHER_RECENT_VERSIONS: '2\.(13|14|15|16)'
      # K3S flags to use for installation
      INSTALL_K3S_SKIP_ENABLE: true
      INSTALL_K3S_VERSION: ${{ inputs.upstream_cluster_version }}
      # For Rancher Manager
      RANCHER_USER: admin
      RANCHER_URL: https://${{ needs.create-runner.outputs.public_dns }}/dashboard
      CERT_MANAGER_VERSION: ${{ inputs.cert-manager_version }}
      PRE_MIGRATION_DEFAULT_RANCHER_VERSION: prime-head/2.12
      POST_MIGRATION_DEFAULT_RANCHER_VERSION: prime-head/2.13
      PRE_UPGRADE_DEFAULT_RANCHER_VERSION: prime-head/2.14
      POST_UPGRADE_DEFAULT_RANCHER_VERSION: prime-head/2.15
      # Qase
      QASE_API_TOKEN: ${{ secrets.qase_api_token }}
      QASE_PROJECT_CODE: RT
      QASE_MODE: ${{ inputs.qase_report == true && 'testops' || 'off' }}
      QASE_TESTOPS_RUN_TITLE: ${{ format('[{4}] Rancher-{0} - {1} destroy={2} dev={3}', inputs.rancher_version, inputs.grep_test_by_tag, inputs.destroy_runner, inputs.turtles_dev_chart, github.run_number) }}
      QASE_TESTOPS_RUN_DESCRIPTION: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
      QASE_TESTOPS_RUN_MILESTONE: ${{ needs.determine-milestone.outputs.milestone }} ### Used only in Summary report
      QASE_TESTOPS_MILESTONE_ID: ${{ needs.determine-milestone.outputs.milestone_id }}
      # Cypress
      TURTLES_REPO: rancher/turtles
      TURTLES_BRANCH: main
      TURTLES_CHART_DEV_VERSION: 108.0.0+up99.99.99
      CONTROLLER_IMG: localhost:5000/rancher/turtles
      BROWSER: chrome
      CYPRESS_DOCKER: 'cypress/included:15.20.0@sha256:5baa6e2f1831e7b9823246d22b5914fe77187a9bab030aa2f44fceabde17c9d1'
      RANCHER_TURTLES_E2E_BRANCH: ${{ github.ref_name }}
      IS_RANCHER_UPGRADED: false
      # Inputs
      GREPTAGS: ${{ inputs.grep_test_by_tag }}
      SKIP_CLUSTER_DELETE: ${{ inputs.skip_cluster_delete }}
      CLUSTER_NAME_SUFFIX: ${{ inputs.cluster_name_suffix }}
      BUILD_TYPE: ${{ inputs.target_build_type }}
      TURTLES_DEV_CHART: ${{ inputs.turtles_dev_chart }}
      SKIP_FLEET_ADDON_INSTALLATION: ${{ inputs.skip_fleet_addon_installation }}
      # Secrets
      AWS_ACCESS_KEY_ID: ${{ secrets.aws_access_key }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.aws_secret_key }}
      GCP_CREDENTIALS: ${{ secrets.gcp_credentials }}
      GCP_PROJECT: ${{ secrets.gcp_project }}
      AZURE_CLIENT_SECRET: ${{ secrets.azure_client_secret }}
      AZURE_CLIENT_ID: ${{ secrets.azure_client_id }}
      AZURE_TENANT_ID: ${{ secrets.azure_tenant_id }}
      AZURE_SUBSCRIPTION_ID: ${{ secrets.azure_subscription_id }}
      VSPHERE_SECRETS_JSON_BASE64: ${{ secrets.vsphere_secrets_json_base64 }}
      DOCKER_AUTH_USERNAME: ${{ secrets.docker_auth_username }}
      DOCKER_AUTH_PASSWORD: ${{ secrets.docker_auth_password }}
      DOCKER_REGISTRY_CONFIG: ${{ secrets.docker_registry_config }}
      PRIME_REGISTRY: ${{ secrets.prime_registry }}
      STG_PRIME_REGISTRY: ${{ secrets.stg_prime_registry }}
      PRIME_ARTIFACTS_URL: ${{ secrets.prime_artifacts_url }}

      # Script to zip and encrypt artifacts
      ARTIFACT_ENCRYPTOR: ${{ github.workspace }}/.github/scripts/encrypt-artifact.sh

      # Common specs: @short @vsphere @full
      SPECS: |
        e2e/providers_setup_legacy.spec.ts
        e2e/providers_setup.spec.ts
        e2e/capd_kubeadm_nocaapf_clusterclass.spec.ts
        e2e/capd_rke2_nocaapf_clusterclass.spec.ts
        e2e/capa_kubeadm_nocaapf_clusterclass.spec.ts
        e2e/capa_eks_nocaapf_clusterclass.spec.ts
        e2e/capa_rke2_nocaapf_clusterclass.spec.ts
        e2e/capz_aks_nocaapf_clusterclass.spec.ts
        e2e/capz_kubeadm_nocaapf_clusterclass.spec.ts
        e2e/capz_rke2_nocaapf_clusterclass.spec.ts
        e2e/capg_kubeadm_nocaapf_clusterclass.spec.ts
        e2e/capg_gke_nocaapf_clusterclass.spec.ts
        e2e/capv_rke2_nocaapf_clusterclass.spec.ts
        e2e/capv_kubeadm_nocaapf_clusterclass.spec.ts
        e2e/fleet_addon_provider_setup.spec.ts
        e2e/capd_rke2_clusterclass.spec.ts
        e2e/capd_rke2_fleet_clusterclass.spec.ts
        e2e/capd_kubeadm_clusterclass.spec.ts
        e2e/azure_rke2_v2prov_cluster.spec.ts
        e2e/capv_rke2_clusterclass.spec.ts
        e2e/capv_kubeadm_clusterclass.spec.ts
        e2e/capa_rke2_clusterclass.spec.ts
        e2e/capa_kubeadm_clusterclass.spec.ts
        e2e/capa_eks_clusterclass.spec.ts
        e2e/capg_gke_clusterclass.spec.ts
        e2e/capg_kubeadm_clusterclass.spec.ts
        e2e/capz_aks_clusterclass.spec.ts
        e2e/capz_rke2_clusterclass.spec.ts
        e2e/capz_kubeadm_clusterclass.spec.ts
        e2e/capi_features_switch.spec.ts
        e2e/capd_kubeadm_use_caapf_switch.spec.ts
    steps:
      - name: Add /usr/local/bin into PATH
        run: |
          echo "/usr/local/bin/" >> ${GITHUB_PATH}
          echo 'Defaults secure_path="/usr/sbin:/usr/bin:/sbin:/bin:/usr/local/bin"' | sudo tee /etc/sudoers.d/0-custom_secure_path
      # To be removed if hitting pull ratelimits
      - name: Log in to Docker Hub
        uses: docker/login-action@dbcb813823bdd20940b903addbd779551569679f # v4.6.0
        with:
          username: ${{ secrets.docker_auth_username }}
          password: ${{ secrets.docker_auth_password }}
      - name: Install helm
        run: |
          HELM_VERSION="3.22.0"
          HELM_SHA256="1e4ab49e429626cf6c6958d914248b78c9730803c2751b87627e171dc800e7bb"
          curl -sk https://get.helm.sh/helm-v${HELM_VERSION}-linux-amd64.tar.gz -o helm.tar.gz
          echo "${HELM_SHA256}  helm.tar.gz" | sha256sum -c -
          sudo tar -xzf helm.tar.gz --strip-components=1 -C /usr/local/bin/ linux-amd64/helm && rm helm.tar.gz

      - name: Set Rancher Manager version and COMMON_SPECS for Pre-Migration
        if: ${{ contains(inputs.grep_test_by_tag, '@migration') }}
        run: |
          echo "COMMON_SPECS=$(echo "${{ env.SPECS }}" | tr '\n' ' ')" >> ${GITHUB_ENV}
          echo "SPECS=" >> ${GITHUB_ENV}
          RANCHER_VERSION="${{ inputs.rancher_version }}"
          # Pre-Migration
          if [[ "$RANCHER_VERSION" == *"2.12"* ]]; then
            echo "RANCHER_VERSION=$RANCHER_VERSION" >> "$GITHUB_ENV"
          elif [[ "$RANCHER_VERSION" == head/* ]]; then
            # Default version for pre-migration tests
            echo "RANCHER_VERSION=${{ env.PRE_MIGRATION_DEFAULT_RANCHER_VERSION }}" >> "$GITHUB_ENV"
          else
            echo "ERROR: Migration is only allowed from 2.12 Rancher Version"
            exit 1
          fi

      - name: Set Rancher Manager version and COMMON_SPECS for Pre-Upgrade
        if: ${{ contains(inputs.grep_test_by_tag, '@upgrade') }}
        run: |
          echo "COMMON_SPECS=$(echo "${{ env.SPECS }}" | tr '\n' ' ')" >> ${GITHUB_ENV}
          RANCHER_VERSION="${{ inputs.rancher_version }}"
          # Pre-Upgrade
          if [[ "$RANCHER_VERSION" == *"2.13"* || "$RANCHER_VERSION" == *"2.14"* ]]; then
            echo "RANCHER_VERSION=$RANCHER_VERSION" >> "$GITHUB_ENV"
          elif [[ "$RANCHER_VERSION" == head/* ]]; then
            # Default version for pre-upgrade tests
            echo "RANCHER_VERSION=${{ env.POST_MIGRATION_DEFAULT_RANCHER_VERSION }}" >> "$GITHUB_ENV"
          else
            echo "ERROR: Upgrade is only allowed from 2.13, 2.14 Rancher Version"
            exit 1
          fi

      - name: Set Rancher Manager version for Installation
        if: ${{ (!contains(inputs.grep_test_by_tag, 'migration')) || (!contains(inputs.grep_test_by_tag, '@upgrade')) }}
        run: |
          # Default version for tests
          echo "RANCHER_VERSION=${{ inputs.rancher_version }}" >> ${GITHUB_ENV}

      - name: Set Rancher Manager point version
        if: ${{ inputs.turtles_dev_chart == true }}
        run: |
          # Rancher 2.12
          if [[ "${{env.RANCHER_VERSION }}" =~ 2.12 ]]; then
            echo RANCHER_POINT_VERSION="2.12" >> ${GITHUB_ENV}
          fi

          # Rancher 2.13 onwards
          if [[ "${{ env.RANCHER_VERSION }}" =~ $RANCHER_RECENT_VERSIONS ]]; then
            echo RANCHER_POINT_VERSION="${BASH_REMATCH[0]}" >> ${GITHUB_ENV}
          fi

          # Migration tests
          if [[ "${{ inputs.grep_test_by_tag }}" =~ "@migration" ]]; then
            echo RANCHER_POINT_VERSION="2.13" >> ${GITHUB_ENV}
          fi

          # Upgrade tests
          if [[ "${{ inputs.grep_test_by_tag }}" =~ "@upgrade" ]]; then
            if [[ "${{ env.RANCHER_VERSION }}" =~ 2.13 ]]; then
              echo RANCHER_POINT_VERSION="2.14" >> ${GITHUB_ENV}
            elif [[ "${{ env.RANCHER_VERSION }}" =~ 2.14 ]]; then
              echo RANCHER_POINT_VERSION="2.15" >> ${GITHUB_ENV}
            fi
          fi
      - name: Set rancher/turtles repo branch
        if: ${{ inputs.turtles_dev_chart == true }}
        run: |

          # Migration tests
          if [[ "${{ inputs.grep_test_by_tag }}" =~ "@migration" ]]; then
            echo "TURTLES_BRANCH=release/v0.25" >> ${GITHUB_ENV}

          # Upgrade tests
          elif [[ "${{ inputs.grep_test_by_tag }}" =~ "@upgrade" ]]; then
            if [[ "${{ env.RANCHER_VERSION }}" =~ 2.13 ]]; then
              echo "TURTLES_BRANCH=release/v0.26" >> ${GITHUB_ENV}
            elif [[ "${{ env.RANCHER_VERSION }}" =~ 2.14 ]]; then
              echo "TURTLES_BRANCH=release/v0.27" >> ${GITHUB_ENV}
            fi

          # Fresh-install tests
          else
            if [[ "${{ env.RANCHER_VERSION }}" =~ 2.12 ]]; then
              echo "TURTLES_BRANCH=release-0.24" >> ${GITHUB_ENV}
            elif [[ "${{ env.RANCHER_VERSION }}" =~ 2.13 ]]; then
              echo "TURTLES_BRANCH=release/v0.25" >> ${GITHUB_ENV}
            elif [[ "${{ env.RANCHER_VERSION }}" =~ 2.14 ]]; then
              echo "TURTLES_BRANCH=release/v0.26" >> ${GITHUB_ENV}
            elif [[ "${{ env.RANCHER_VERSION }}" =~ 2.15 ]]; then
              echo "TURTLES_BRANCH=release/v0.27" >> ${GITHUB_ENV}
            else
              echo "TURTLES_BRANCH=${{ env.TURTLES_BRANCH }}" >> ${GITHUB_ENV}
            fi          
          fi
      - name: Set k3s Install version
        run: |
          if [[ "${{ env.RANCHER_VERSION }}" =~ (2\.12) ]]; then
            echo INSTALL_K3S_VERSION="v1.33.4+k3s1" >> ${GITHUB_ENV}
          else
            echo INSTALL_K3S_VERSION=${{ inputs.upstream_cluster_version }} >> ${GITHUB_ENV}
          fi

      # Checkout into r/turtles repo if dev=true to build turtles and providers chart;
      # or if it's a migration test, then to copy the provider migration script
      - name: Check out rancher/turtles repo
        if: ${{ inputs.turtles_dev_chart == true || contains(inputs.grep_test_by_tag, '@migration') }}
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          repository: ${{ env.TURTLES_REPO }}
          ref: ${{ env.TURTLES_BRANCH }}
      - name: Install Go
        if: ${{ inputs.turtles_dev_chart == true }}
        uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          cache: false
          go-version-file: go.mod

      - name: Set date variable
        run: |
          echo "TAG=$(date "+%Y.%m.%d")" >> ${GITHUB_ENV}

      # This step builds latest turtles chart and pushes latest turtles docker image to local docker registry
      - name: Make Turtles chart
        if: ${{ inputs.turtles_dev_chart == true }}
        run: |
          # Image injection for dev=true was moved to tests/e2e/install_test.go (rancher-config patching).
          TARGET_BUILD=${{ env.BUILD_TYPE }} TAG=v${{ env.TAG }} CONTROLLER_IMG=${{ env.CONTROLLER_IMG }} make e2e-image-build
          RELEASE_TAG=v${{ env.TAG }} CONTROLLER_IMG=${{ env.CONTROLLER_IMG }} CONTROLLER_IMAGE_VERSION=v${{ env.TAG }} make build-chart # verify-gen
          docker run -d -p 5000:5000 --name registry registry:2
          docker push ${{ env.CONTROLLER_IMG }}:v${{ env.TAG }}

      # Build providers chart to push to chartmuseum in a later step
      - name: Make Turtles Providers Chart
        if: ${{ inputs.turtles_dev_chart == true && (!contains(env.RANCHER_POINT_VERSION, '2.12')) }}
        run: RELEASE_TAG=v${{ env.TAG }} make build-providers-chart

      - name: Clone, build and publish artificial system charts for 2.13 onwards
        if: ${{ inputs.turtles_dev_chart == true && (!contains(env.RANCHER_POINT_VERSION, '2.12')) }}
        run: |
          # Dir outside github workspace must be used for destroy=false persistance
          export RANCHER_CHARTS_REPO_DIR=/tmp/system-charts/charts
          export RANCHER_CHART_DEV_VERSION=${{ env.TURTLES_CHART_DEV_VERSION }}
          export RANCHER_CHARTS_BASE_BRANCH=dev-v${{ env.RANCHER_POINT_VERSION }}
          export CHART_RELEASE_DIR=${{ github.workspace }}/out/charts/rancher-turtles
          export HELM=helm

          # Script from rancher/turtles
          chmod +x scripts/build-local-rancher-charts.sh
          ./scripts/build-local-rancher-charts.sh

          # Note allow port tcp/4080 by a firewall rule on GCP
          docker run --name git-http-backend -d -p 4080:80 -v /tmp/system-charts:/git ynohat/git-http-backend

      - name: Copy Migration Script (to be run after migration from 2.12 to 2.13)
        if: ${{ contains(inputs.grep_test_by_tag, '@migration') }}
        run: |
          # Migration script from rancher/turtles
          cp scripts/migrate-providers-ownership.sh /tmp
          chmod +x /tmp/migrate-providers-ownership.sh

      # Copy chart files from turtles repo to local system/runner
      - name: Copy turtles chart files
        if: ${{ inputs.turtles_dev_chart == true }}
        run: |
          cp ${{ github.workspace }}/out/package/rancher-turtles-${{ env.TAG }}.tgz ${{ runner.temp }}

      - name: Copy Turtles Providers chart files
        if: ${{ inputs.turtles_dev_chart == true && (!contains(env.RANCHER_POINT_VERSION, '2.12')) }}
        run: cp ${{ github.workspace }}/out/package/rancher-turtles-providers-${{ env.TAG }}.tgz ${{ runner.temp }}

      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Install Go
        uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          cache: false
          go-version-file: tests/go.mod
      - name: Copy turtles chart files for chartmuseum
        if: ${{ inputs.turtles_dev_chart == true }}
        run: |
          cp ${{ runner.temp }}/rancher-turtles-${{ env.TAG }}.tgz ${{ github.workspace }}/tests/assets
      - name: Copy turtles providers chart files for chartmuseum
        if: ${{ inputs.turtles_dev_chart == true && (!contains(env.RANCHER_POINT_VERSION, '2.12')) }}
        run: |
          mkdir ${{ github.workspace }}/tests/assets/providers
          cp ${{ runner.temp }}/rancher-turtles-providers-${{ env.TAG }}.tgz ${{ github.workspace }}/tests/assets/providers
      # Copy migration script from tmp to r/rancher-turtles-e2e workspace
      - name: Copy Migration Script to r/rancher-turtles-e2e workspace
        if: ${{ contains(inputs.grep_test_by_tag, '@migration') }}
        run: cp /tmp/migrate-providers-ownership.sh ${{ github.workspace }}/tests/assets/
      - name: Inject kube-vip controlplane IP for vsphere into capv-helm-values.yaml
        if: contains(needs.create-runner.outputs.uuid, 'vsphere')
        run: |
          # A round-robin distribution of 3 available control plane IPs for kube-vip
          INDEX=$(((${{ github.run_number }} % 3) + 1))
          CP_ENDPOINT_IP=$(echo ${{ secrets.vsphere_endpoints_list }} | cut -d' ' -f${INDEX})
          sed -i "s/replace_cluster_control_plane_endpoint_ip/${CP_ENDPOINT_IP}/" tests/cypress/latest/fixtures/vsphere/capv-helm-values.yaml
      - name: Get Rancher prime-head tag for Install
        if: ${{ contains(env.RANCHER_VERSION, 'prime-head') }}
        run: |
          HEAD_VERSION="${RANCHER_VERSION#prime-head/}"
          RM_VERSION=$(curl -fsSL ${{ secrets.prime_head_chart_url }} | yq ".entries.rancher | map(select(.version | test(\"^${HEAD_VERSION}\"))) | sort_by(.created) | .[-1].version")
          echo "RANCHER_VERSION=prime-rc/${RM_VERSION}" >> ${GITHUB_ENV}
      - name: Install Rancher Manager
        env:
          RANCHER_VERSION: ${{ env.RANCHER_VERSION }}
          PUBLIC_DNS: ${{ needs.create-runner.outputs.public_dns }}
        run: cd tests && make e2e-install-rancher
      - name: Extract component versions/information
        id: component
        run: |
          # Extract CertManager image version
          CERT_MANAGER_IMAGE_VERSION=$(kubectl get pod \
                                   --namespace cert-manager \
                                   -l app=cert-manager \
                                   -o jsonpath={.items[*].status.containerStatuses[*].image} 2> /dev/null || true)
          # Extract Rancher Manager image version
          RM_IMAGE_VERSION=$(kubectl get pod \
                         --namespace cattle-system \
                         -l app=rancher \
                         -o jsonpath={.items[*].status.containerStatuses[*].image} 2> /dev/null || true)
          # Export values
          echo "cert_manager_image_version=${CERT_MANAGER_IMAGE_VERSION}" >> ${GITHUB_OUTPUT}
          echo "rm_image_version=${RM_IMAGE_VERSION}" >> ${GITHUB_OUTPUT}
          echo "rm_version=${{ env.RANCHER_VERSION }}" >> ${GITHUB_OUTPUT}
      - name: Install Chartmuseum
        if: ${{ inputs.turtles_dev_chart == true }}
        run: cd tests && make e2e-install-chartmuseum
      - name: Setup node
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: current
      - name: Cypress tests
        id: cypress
        env:
          RANCHER_TURTLES_E2E_BRANCH: ${{ env.RANCHER_TURTLES_E2E_BRANCH }}
          RANCHER_VERSION: ${{ env.RANCHER_VERSION }}
          RANCHER_PASSWORD: ${{ secrets.rancher_password }}
          SPEC: |
            e2e/first_connection.spec.ts
            e2e/turtles_chart_legacy.spec.ts
            e2e/turtles_ui_extension.spec.ts
            e2e/menu.spec.ts
            ${{ env.SPECS }}
            e2e/capd_rke2_migration_clusterclass.spec.ts
            e2e/capd_rke2_upgrade_clusterclass.spec.ts
            e2e/pre_upgrade_setup.spec.ts
        run: |
          if [ ${{ inputs.turtles_dev_chart }} == true ]; then
            export CHARTMUSEUM_REPO=http://${{ needs.create-runner.outputs.public_dns }}
          fi
          cd tests && make start-cypress-tests
      - name: Set milestone_id for current run and store QASE_TESTOPS_RUN_ID for later
        if: ${{ always() && inputs.qase_report == true }}
        id: qase_run_id
        run: |
          if [ -f tests/cypress/latest/QASE_TESTOPS_RUN_ID.txt ]; then
            QASE_TESTOPS_RUN_ID=$(cat tests/cypress/latest/QASE_TESTOPS_RUN_ID.txt)
            echo "qase_run_id=${QASE_TESTOPS_RUN_ID}" >> ${GITHUB_OUTPUT}
          fi
          ## Set milestone_id for the current run
          if [ -n "${QASE_TESTOPS_RUN_ID}" ] && [ -n "${QASE_TESTOPS_MILESTONE_ID}" ]; then
            STATUS=$(curl -s --request PATCH \
              --url "https://api.qase.io/v1/run/${QASE_PROJECT_CODE}/${QASE_TESTOPS_RUN_ID}" \
              --header "Token: ${QASE_API_TOKEN}" \
              --header "Content-Type: application/json" \
              --header "accept: application/json" \
              --data "{\"milestone_id\":${QASE_TESTOPS_MILESTONE_ID}}" \
              | jq -r '.status // empty')
            if [ "$STATUS" != "true" ]; then
              echo "Failed to set milestone ID ${QASE_TESTOPS_MILESTONE_ID} for run ${QASE_TESTOPS_RUN_ID}"
            fi
          fi
      - name: Stage Cypress screenshots for upload
        if: ${{ failure() && steps.cypress.outcome != 'skipped' }}
        run: |
          # Cypress only creates this directory once it writes a failure screenshot,
          # and a failure before the Cypress step means it never existed.
          [ -d tests/cypress/latest/screenshots ] || exit 0
          mkdir -p ${{ runner.temp }}/logs-and-screenshots
          mv tests/cypress/latest/screenshots ${{ runner.temp }}/logs-and-screenshots/screenshots
      - name: Encrypt Cypress videos
        # Test run video is always captured, so this step uses "always()" condition
        if: ${{ always() && steps.cypress.outcome != 'skipped' }}
        env:
          LOG_ENCRYPTION_KEY: ${{ secrets.log_encryption_key }}
        run: |
          bash ${{ env.ARTIFACT_ENCRYPTOR }} \
            tests/cypress/latest/videos \
            ${{ runner.temp }}/cypress-videos-${{ github.run_number }}.tar.gpg
      - name: Upload encrypted Cypress videos
        if: ${{ always() && steps.cypress.outcome != 'skipped' }}
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: cypress-videos-${{ github.run_number }}.tar.gpg
          path: ${{ runner.temp }}/cypress-videos-${{ github.run_number }}.tar.gpg
          retention-days: 7
          archive: 'false'
          if-no-files-found: ignore
      - name: Set Rancher Manager version for Migration
        if: ${{ contains(inputs.grep_test_by_tag, '@migration') }}
        run: |
          RANCHER_UPGRADE_VERSION="${{ inputs.rancher_upgrade_version }}"
          # Post-Migration
          if [[ "$RANCHER_VERSION" == *"2.12"* && "$RANCHER_UPGRADE_VERSION" == *"2.13"* ]]; then
            echo "RANCHER_VERSION=$RANCHER_UPGRADE_VERSION" >> "$GITHUB_ENV"
          elif [ -z "$RANCHER_UPGRADE_VERSION" ]; then
            # Default version for post-migration tests
            echo "RANCHER_VERSION=${{ env.POST_MIGRATION_DEFAULT_RANCHER_VERSION }}" >> "$GITHUB_ENV"
          else
            echo "ERROR: Migration is only allowed to 2.13 Rancher Version"
            exit 1
          fi
      - name: Set Rancher Manager version for Post-Upgrade
        if: ${{ contains(inputs.grep_test_by_tag, '@upgrade') }}
        run: |
          RANCHER_UPGRADE_VERSION="${{ inputs.rancher_upgrade_version }}"
          # Post-Upgrade
          if [[ "$RANCHER_VERSION" == *"2.13"* && "$RANCHER_UPGRADE_VERSION" == *"2.14"* || "$RANCHER_VERSION" == *"2.14"* && "$RANCHER_UPGRADE_VERSION" == *"2.15"* ]]; then
            echo "RANCHER_VERSION=$RANCHER_UPGRADE_VERSION" >> "$GITHUB_ENV"
          elif [ -z "$RANCHER_UPGRADE_VERSION" ]; then
            # Default version for post-upgrade tests
            echo "RANCHER_VERSION=${{ env.POST_UPGRADE_DEFAULT_RANCHER_VERSION }}" >> "$GITHUB_ENV"
          else
            echo "ERROR: Upgrade is only allowed to 2.14, 2.15 Rancher Versions"
            exit 1
          fi
      - name: Get Rancher prime-head tag for Upgrade
        if: ${{ contains(env.RANCHER_VERSION, 'prime-head') && (contains(inputs.grep_test_by_tag, '@migration') || contains(inputs.grep_test_by_tag, '@upgrade')) }}
        run: |
          HEAD_VERSION="${RANCHER_VERSION#prime-head/}"
          RM_VERSION=$(curl -fsSL ${{ secrets.prime_head_chart_url }} | yq ".entries.rancher | map(select(.version | test(\"^${HEAD_VERSION}\"))) | sort_by(.created) | .[-1].version")
          echo "RANCHER_VERSION=prime-rc/${RM_VERSION}" >> ${GITHUB_ENV}
      - name: Upgrade Rancher Manager
        if: ${{ contains(inputs.grep_test_by_tag, '@migration') || contains(inputs.grep_test_by_tag, '@upgrade') }}
        env:
          RANCHER_VERSION: ${{ env.RANCHER_VERSION }}
          KUBECONFIG: /etc/rancher/k3s/k3s.yaml
          PUBLIC_DNS: ${{ needs.create-runner.outputs.public_dns }}
        run: cd tests && make e2e-upgrade-rancher
      - name: Extract component versions/information after Rancher Upgrade
        id: upgraded_component
        if: ${{ contains(inputs.grep_test_by_tag, '@migration') || contains(inputs.grep_test_by_tag, '@upgrade') }}
        run: |
          # Extract Rancher Manager image version
          RM_UPGRADED_IMAGE_VERSION=$(kubectl get pod \
            --namespace cattle-system \
            -l app=rancher \
            -o jsonpath={.items[*].status.containerStatuses[*].image} 2> /dev/null || true)
          # Export values
          echo "rm_upgraded_image_version=${RM_UPGRADED_IMAGE_VERSION}" >> ${GITHUB_OUTPUT}
          echo "rm_upgraded_version=${{ env.RANCHER_VERSION }}" >> ${GITHUB_OUTPUT}
      - name: Migration script
        if: ${{ contains(inputs.grep_test_by_tag, '@migration') }}
        run: |
          ${{ github.workspace }}/tests/assets/migrate-providers-ownership.sh --adopt docker:capd-system
      - name: Cypress tests - Upgraded Rancher
        id: cypress_upgraded
        if: ${{ contains(inputs.grep_test_by_tag, '@migration') || contains(inputs.grep_test_by_tag, '@upgrade') }}
        env:
          RANCHER_TURTLES_E2E_BRANCH: ${{ env.RANCHER_TURTLES_E2E_BRANCH }}
          RANCHER_VERSION: ${{ env.RANCHER_VERSION }}
          RANCHER_PASSWORD: ${{ secrets.rancher_password }}
          QASE_TESTOPS_RUN_ID: ${{ steps.qase_run_id.outputs.qase_run_id }}
          IS_RANCHER_UPGRADED: true
          SPEC: |
            e2e/capd_rke2_migration_clusterclass.spec.ts
            e2e/post_upgrade_setup.spec.ts
            ${{ env.COMMON_SPECS }}
            e2e/capd_rke2_upgrade_clusterclass.spec.ts
        run: |
          if [ ${{ inputs.turtles_dev_chart }} == true ]; then
            export CHARTMUSEUM_REPO=http://${{ needs.create-runner.outputs.public_dns }}
          fi
          cd tests && make start-cypress-tests
      - name: Stage Cypress screenshots (after-upgrade) for upload
        if: ${{ failure() && steps.cypress_upgraded.outcome != 'skipped' }}
        run: |
          # Absent when the pre-upgrade run already staged them, or when the
          # upgraded-Rancher Cypress step was skipped because that run failed.
          [ -d tests/cypress/latest/screenshots ] || exit 0
          mkdir -p ${{ runner.temp }}/logs-and-screenshots
          mv tests/cypress/latest/screenshots ${{ runner.temp }}/logs-and-screenshots/screenshots-after-upgrade
      - name: Encrypt Cypress videos (after-upgrade)
        # Test run video is always captured, so this step uses "always()" condition
        if: ${{ always() && steps.cypress_upgraded.outcome != 'skipped' }}
        env:
          LOG_ENCRYPTION_KEY: ${{ secrets.log_encryption_key }}
        run: |
          bash ${{ env.ARTIFACT_ENCRYPTOR }} \
            tests/cypress/latest/videos \
            ${{ runner.temp }}/cypress-videos-after-upgrade-${{ github.run_number }}.tar.gpg
      - name: Upload encrypted Cypress videos (after-upgrade)
        if: ${{ always() && steps.cypress_upgraded.outcome != 'skipped' }}
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: cypress-videos-after-upgrade-${{ github.run_number }}.tar.gpg
          path: ${{ runner.temp }}/cypress-videos-after-upgrade-${{ github.run_number }}.tar.gpg
          retention-days: 7
          archive: 'false'
          if-no-files-found: ignore
      - name: Collect cluster logs
        env:
          KUBECONFIG: /etc/rancher/k3s/k3s.yaml
          # Environment variable keys whose values are excluded from collected artifact data.
          SECRET_KEYS_TO_MASK: RANCHER_HOSTNAME,RANCHER_PASSWORD,GCP_PROJECT,GCP_CREDENTIALS,AWS_ACCESS_KEY_ID,AWS_SECRET_ACCESS_KEY,AZURE_SUBSCRIPTION_ID,AZURE_CLIENT_ID,AZURE_CLIENT_SECRET,AZURE_TENANT_ID,AZURE_SUBSCRIPTION_ID,DOCKER_AUTH_USERNAME,DOCKER_AUTH_PASSWORD,DOCKER_REGISTRY_CONFIG,VSPHERE_SECRETS_JSON_BASE64,GH_TOKEN
          RANCHER_LOG_COLLECTOR: ${{ github.workspace }}/.github/scripts/collect-rancher-logs.sh
          RANCHER_PASSWORD: ${{ secrets.rancher_password }}
        if: always()
        run: bash ${{ env.RANCHER_LOG_COLLECTOR }}
      - name: Stage cluster logs for upload
        # Kept separate from collection: the collector is `set -e`, so if it dies
        # partway (curl, checksum, crust-gather) we still want whatever it wrote.
        if: always()
        run: |
          [ -d ${{ github.workspace }}/logs ] || exit 0
          mkdir -p ${{ runner.temp }}/logs-and-screenshots
          mv ${{ github.workspace }}/logs ${{ runner.temp }}/logs-and-screenshots/logs
      - name: Encrypt screenshots and cluster logs
        if: always()
        env:
          LOG_ENCRYPTION_KEY: ${{ secrets.log_encryption_key }}
        run: |
          bash ${{ env.ARTIFACT_ENCRYPTOR }} \
            ${{ runner.temp }}/logs-and-screenshots \
            ${{ runner.temp }}/logs-and-screenshots-${{ github.run_number }}.tar.gpg
      - name: Upload encrypted screenshots and cluster logs
        if: ${{ always() }}
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: logs-and-screenshots-${{ github.run_number }}.tar.gpg
          path: ${{ runner.temp }}/logs-and-screenshots-${{ github.run_number }}.tar.gpg
          retention-days: 7
          archive: 'false'
          if-no-files-found: ignore
      - name: Add summary
        env:
          KUBECONFIG: /etc/rancher/k3s/k3s.yaml
        if: ${{ always() }}
        run: |
          # Add summary
          echo "## General information" >> ${GITHUB_STEP_SUMMARY}
          echo -e "***${{ inputs.test_description }}***\n" >> ${GITHUB_STEP_SUMMARY}
          echo "### Rancher Manager & Turtles Information" >> ${GITHUB_STEP_SUMMARY}
          echo "Rancher Manager Image: ${{ steps.component.outputs.rm_image_version }}" >> ${GITHUB_STEP_SUMMARY}
          echo "Rancher Manager Version: ${{ steps.component.outputs.rm_version }}" >> ${GITHUB_STEP_SUMMARY}
          echo "Rancher Manager Milestone: ${{ needs.determine-milestone.outputs.milestone }}" >> ${GITHUB_STEP_SUMMARY}
          echo "CertManager Image: ${{ steps.component.outputs.cert_manager_image_version }}" >> ${GITHUB_STEP_SUMMARY}
          if [[ "${{ env.RANCHER_VERSION }}" =~ $RANCHER_RECENT_VERSIONS ]]; then
            TURTLES_NAMESPACE="cattle-turtles-system"
          else
            TURTLES_NAMESPACE="rancher-turtles-system"
          fi
          TURTLES_VERSION=$(helm list -n $TURTLES_NAMESPACE -o json 2> /dev/null | jq -r '.[] | .chart' | head -n 1)
          echo "Rancher Turtles Installed/Upgraded Chart Version: $TURTLES_VERSION" >> ${GITHUB_STEP_SUMMARY}
          if [[ ${{ inputs.turtles_dev_chart }} == true ]]; then
            echo "Rancher Turtles Dev Build Type: ${{ inputs.target_build_type }}" >> ${GITHUB_STEP_SUMMARY}
          fi
          TURTLES_PROVIDERS_VERSION=$(helm list -n $TURTLES_NAMESPACE -o json 2> /dev/null | jq -r '.[] | .chart' | tail -n 1)
          echo "Rancher Turtles Providers Chart Version: $TURTLES_PROVIDERS_VERSION" >> ${GITHUB_STEP_SUMMARY}
          # Only add provider section if providers exist
          PROVIDER_DATA=$(kubectl get capiproviders.turtles-capi.cattle.io -A \
            -o jsonpath='{range .items[*]}{.metadata.name}{" "}{.metadata.namespace}{" "}{.spec.version}{"\n"}{end}' 2>/dev/null)
          # Format provider data into a markdown table
          if [[ -n "$PROVIDER_DATA" ]]; then
            PROVIDER_VERSIONS=$(
              printf "| NAME | NAMESPACE | VERSION |\n|:---|:---|:---|\n"
              echo "$PROVIDER_DATA" | awk '{printf "| %-25s | %-35s | %-8s |\n", $1, $2, $3}'
            )
            echo "Installed CAPI Providers:" >> ${GITHUB_STEP_SUMMARY}
            echo "${PROVIDER_VERSIONS}" >> ${GITHUB_STEP_SUMMARY}
          fi
          if [[ "${{ inputs.grep_test_by_tag }}" =~ "@migration" || "${{ inputs.grep_test_by_tag }}" =~ "@upgrade" ]]; then
            echo "### Rancher Manager Upgrade Information" >> ${GITHUB_STEP_SUMMARY}
            echo "Rancher Manager Upgraded Version: ${{ steps.upgraded_component.outputs.rm_upgraded_version }}" >> ${GITHUB_STEP_SUMMARY}
            echo "Rancher Manager Upgraded Image: ${{ steps.upgraded_component.outputs.rm_upgraded_image_version }}" >> ${GITHUB_STEP_SUMMARY}
          fi
          echo "### Kubernetes" >> ${GITHUB_STEP_SUMMARY}
          echo "K3s on Rancher Manager: ${{ env.INSTALL_K3S_VERSION }}" >> ${GITHUB_STEP_SUMMARY}
          if [[ -n "${{ steps.qase_run_id.outputs.qase_run_id }}" ]]; then
            echo -e "\n### QASE Report" >> ${GITHUB_STEP_SUMMARY}
            echo "View the detailed report [here](https://app.qase.io/run/RT/dashboard/${{ steps.qase_run_id.outputs.qase_run_id }})" >> ${GITHUB_STEP_SUMMARY}
          else
            echo -e "\n### Test not reported in QASE!" >> ${GITHUB_STEP_SUMMARY}
          fi

  delete-runner:
    if: ${{ always() && contains(needs.create-runner.outputs.uuid, 'vsphere') == false }}
    needs: [create-runner, e2e]
    runs-on: ubuntu-latest
    steps:
      # actions/checkout MUST come before auth
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Authenticate to GCP
        uses: google-github-actions/auth@7c6bc770dae815cd3e89ee6cdf493a5fab2cc093 # v3.0.0
        with:
          credentials_json: ${{ secrets.gcp_credentials }}
      - name: Setup gcloud
        uses: google-github-actions/setup-gcloud@aa5489c8933f4cc7a4f7d45035b3b1440c9c10db # v3.0.1
      - name: Delete GCP secrets
        run: |
          gcloud --quiet secrets delete PAT_TOKEN_${{ needs.create-runner.outputs.uuid }} || true
          gcloud --quiet secrets delete GH_REPO_${{ needs.create-runner.outputs.uuid }} || true
      - name: Delete runner
        if: ${{ always() && inputs.destroy_runner == true }}
        run: |
          gcloud --quiet compute instances delete ${{ needs.create-runner.outputs.runner }} \
            --delete-disks all \
            --zone ${{ inputs.zone }}

  # Just to signify that something has been cancelled and it's not useful to check the test
  declare-cancelled:
    if: ${{ always() && contains(needs.e2e.result, 'cancelled') }}
    needs: e2e
    runs-on: ubuntu-latest
    steps:
      - name: Specify in summary if something has been cancelled
        run: echo "# TEST CANCELLED!" >> ${GITHUB_STEP_SUMMARY}
      - name: Complete the QASE run if cancelled
        # Otherwise the cancelled run would stay in "In Progress" state
        run: |
          RUN_ID=${{ needs.e2e.outputs.qase_run_id }}
          if [ -n "$RUN_ID" ]; then
            curl -X POST --data-raw="" \
              --url https://api.qase.io/v1/run/RT/${RUN_ID}/complete \
              -H "Token: ${{ secrets.QASE_API_TOKEN }}" \
              -H 'accept: application/json'
          fi
          echo "QASE run $RUN_ID marked as Completed" >> ${GITHUB_STEP_SUMMARY}
  cleanup-vsphere-runner:
    if: ${{ always() && contains(needs.create-runner.outputs.uuid, 'vsphere') && inputs.destroy_runner == true }}
    needs: [create-runner, e2e]
    runs-on: ${{ needs.create-runner.outputs.uuid }}
    steps:
      - name: Cleanup vsphere runner
        env:
          WORKSPACE: ${{ github.workspace }}
        run: cd tests && make vsphere-runner-cleanup

  report-pr-result:
    if: always()
    needs: [e2e]
    runs-on: ubuntu-latest
    permissions:
      pull-requests: write
    steps:
      - name: Find PR and update description with result
        env:
          GH_TOKEN: ${{ github.token }}
          REPO: ${{ github.repository }}
        run: |
          BRANCH="${{ github.head_ref || github.ref_name }}"

          PR_NUMBER=$(gh pr list --repo "$REPO" --head "$BRANCH" --state open \
            --json number --jq '.[0].number // empty')

          if [[ -z "$PR_NUMBER" ]]; then
            echo "No open PR found for branch $BRANCH, skipping."
            exit 0
          fi

          RESULT="${{ needs.e2e.result }}"
          case "$RESULT" in
            success)   STATUS="✅ Pass" ;;
            failure)   STATUS="❌ Fail" ;;
            cancelled) STATUS="⚠️ Cancelled" ;;
            skipped)   STATUS="⏭️ Skipped" ;;
            *)         STATUS="❓ Unknown" ;;
          esac

          RUN_NAME="[${{ github.run_number }}] Rancher-${{ inputs.rancher_version }} - **${{ inputs.grep_test_by_tag }}** dev=${{ inputs.turtles_dev_chart }} destroy=${{ inputs.destroy_runner }}"
          RUN_URL="${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"
          NEW_ROW="| [${RUN_NAME}](${RUN_URL}) | ${STATUS} | |"

          # Get current PR body
          gh pr view "$PR_NUMBER" --repo "$REPO" --json body --jq '.body' > /tmp/pr_body.txt

          if grep -q '<!-- e2e-result-end -->' /tmp/pr_body.txt; then
            # Append new row before end marker (preserves all existing rows)
            sed -i "/<!-- e2e-result-end -->/i\\${NEW_ROW}" /tmp/pr_body.txt
          else
            # Fallback for legacy PRs without markers: append full section
            cat >> /tmp/pr_body.txt <<EOF

          <!-- e2e-result-start -->
          ### E2E Test Results:

          | Run Name | Run Result | Notes |
          |----------|------------|-------|
          ${NEW_ROW}
          <!-- e2e-result-end -->
          EOF
          fi

          gh pr edit "$PR_NUMBER" --repo "$REPO" --body-file /tmp/pr_body.txt
