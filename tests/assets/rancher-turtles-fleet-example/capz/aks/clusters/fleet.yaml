namespace: default

helm:
  valuesFrom:
    - secretKeyRef:
        name: azure-creds-secret
        namespace: capz-system
        key: values.yaml
  values:
    clustername_prefix: turtles-qa-azure-aks
